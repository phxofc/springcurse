server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
spring-doc:
  pathsToMatch: /person/**
  swagger-ui:
    use-root-path: true
